# Run `scripts/run_hybrid_masks.sh`

Start local server:
```
python main.py serve --port 3600
```

Run with a video input:
Args:
`<video_or_frames_dir> <mask_dir> <scene_name> [max_frames] [mask_glob] [mask_offset]`

```
./scripts/run_hybrid_masks.sh 
```

